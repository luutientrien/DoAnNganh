import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	AdminProductDetail: {
		width: '1000px',
		'& .title-product': {
			margin: '20px 0'
		}
	}
}));