import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	AdminSaleInfo: {
		width: 'fit-content',
		padding: '20px',
		margin: 'auto',
		'& .box-table': {

		}
	},
}));
