import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	search: {
		height: 55,
		// width: 120,
	},
	title: {
		textAlign: 'center',
		margin: 30,
	},
	tool: {
		marginLeft: 40,
	},
	Dashboard: {

	}
}));